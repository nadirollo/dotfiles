# ~/.tmuxinator/wks-micro-terraform.yml

name: wks-micro-terraform
root: ~/workspace/wks/micro-terraform

windows:
  - editor:
      layout: main-horizontal
      panes:
        - terraform:
            - docker-compose run terragrunt
        - git:
            -
